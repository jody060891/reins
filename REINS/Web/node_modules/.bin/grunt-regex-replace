#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-regex-replace/bin/grunt-regex-replace" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-regex-replace/bin/grunt-regex-replace" "$@"
  ret=$?
fi
exit $ret
