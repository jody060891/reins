angular.module('PKBL')
    .controller('MaintenanceCtrl',
    function MaintenanceCtrl($rootScope, $scope, $resource, $location, AuthenticationService, InstitutionService, SessionService, ToastMessageService) {

    });
